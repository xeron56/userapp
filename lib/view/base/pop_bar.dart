import 'package:flutter/material.dart';

class Popbar extends StatelessWidget {
  const Popbar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}